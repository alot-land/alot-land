---
import BaseLayout from '../layouts/BaseLayout.astro';
import ListingCard from '../components/ListingCard.astro';
import VideoEmbed from '../components/VideoEmbed.astro';
import TestimonialSection from '../components/TestimonialSection.astro';
import FAQSection from '../components/FAQSection.astro';
import { getCollection, getEntry } from 'astro:content';
import { siteConfig } from '../config/site.js';

const contactSettings  = await getEntry('settings', 'contact');
const homepageSettings = await getEntry('settings', 'homepage');
const phone           = contactSettings?.data.phone           ?? siteConfig.phone;
const phoneRaw        = contactSettings?.data.phoneRaw        ?? siteConfig.phoneRaw;
const homepageVideoId = homepageSettings?.data.homepageVideoId ?? siteConfig.homepageVideoId;
const heroTagline     = homepageSettings?.data.heroTagline     ?? siteConfig.tagline;

const allListings = await getCollection('listings');
// Homepage box = every listing with the "Show on home page" toggle (featured) on,
// regardless of status. Ordered by status (Available first, then Coming Soon,
// Under Contract, Sold), newest-first within each group. No cap — all toggled show.
const statusOrder: Record<string, number> = {
  'available': 0,
  'coming-soon': 1,
  'under-contract': 2,
  'sold': 3,
};
const featuredListings = allListings
  .filter(l => l.data.featured && l.data.status !== 'sold')
  .sort((a, b) => {
    const byStatus = (statusOrder[a.data.status] ?? 9) - (statusOrder[b.data.status] ?? 9);
    return byStatus !== 0 ? byStatus : b.data.date.getTime() - a.data.date.getTime();
  });
---

<BaseLayout
  title="Buy & Sell Land in Middle Tennessee"
  description="Alot of Land LLC — great land at great prices in Middle Tennessee. Browse available properties, request a custom land search, or get a cash offer on yours."
>
  <!-- Hero -->
  <section class="relative min-h-[50vh] flex items-center justify-center text-white overflow-hidden py-20 sm:py-28">
    <img
      src="/images/hero-land.jpg"
      alt="Aerial view of open land at golden hour"
      class="absolute inset-0 w-full h-full object-cover"
    />
    <div class="absolute inset-0 bg-black/60"></div>
    <div class="relative z-10 text-center px-4 max-w-4xl mx-auto">
      <h1 class="font-display font-black text-3xl sm:text-4xl lg:text-5xl mb-6 leading-tight">
        Buy Great Land at Great Prices in Middle Tennessee
      </h1>
      <p class="text-xl sm:text-2xl text-gray-200 mb-10 max-w-2xl mx-auto leading-relaxed">
        {heroTagline}
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center mb-10">
        <a
          href="/listings"
          class="bg-gold text-bg-dark font-bold text-lg px-8 py-4 rounded-2xl hover:bg-yellow-400 transition-colors"
        >
          Browse Land
        </a>
        <a
          href="/sell-land"
          class="bg-white/15 border border-white/40 text-white font-bold text-lg px-8 py-4 rounded-2xl hover:bg-white/25 transition-colors"
        >
          Sell Your Land
        </a>
      </div>
      <a
        href={`tel:${phoneRaw}`}
        class="inline-flex items-center gap-2 text-gray-300 hover:text-white text-lg transition-colors"
      >
        📞 {phone}
      </a>
    </div>
  </section>

  <!-- What We Do -->
  <section class="py-20 bg-bg-light">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid md:grid-cols-2 gap-8 max-w-3xl mx-auto">
        <!-- Buy Land -->
        <div class="bg-white rounded-2xl p-8 shadow-sm border border-gray-100 flex flex-col">
          <div class="text-4xl mb-4">🏡</div>
          <h2 class="font-display font-bold text-2xl text-bg-dark mb-3">Buy Land</h2>
          <p class="text-gray-600 leading-relaxed mb-6 flex-1">
            Looking for land? Browse our available properties or tell us exactly what you want and we'll go find it off-market for you.
          </p>
          <div class="flex flex-col gap-3">
            <a href="/listings" class="text-center bg-bg-dark text-white font-semibold px-5 py-2.5 rounded-xl hover:bg-gray-800 transition-colors text-sm">
              View Listings
            </a>
            <a href="/find-land" class="text-center border border-bg-dark text-bg-dark font-semibold px-5 py-2.5 rounded-xl hover:bg-gray-50 transition-colors text-sm">
              Request a Search
            </a>
          </div>
        </div>

        <!-- Sell Land -->
        <div class="bg-green-brand rounded-2xl p-8 text-white flex flex-col">
          <div class="text-4xl mb-4">💵</div>
          <h2 class="font-display font-bold text-2xl mb-3">Sell Your Land</h2>
          <p class="text-green-100 leading-relaxed mb-6 flex-1">
            Want to sell your land fast? We buy land direct — no agent fees, no commissions, no waiting. Get a cash offer within 24 hours.
          </p>
          <div class="flex flex-col gap-3">
            <a href="/sell-land" class="text-center bg-white text-green-brand font-bold px-5 py-2.5 rounded-xl hover:bg-green-50 transition-colors text-sm">
              Get a Cash Offer
            </a>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- Featured Listings -->
  <section class="py-20 bg-gold">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-end justify-between mb-10">
        <h2 class="font-display font-bold text-3xl sm:text-4xl text-bg-dark">Featured Listings</h2>
        <a href="/listings" class="text-sm font-semibold text-bg-dark font-bold hover:underline hidden sm:block">See All Listings →</a>
      </div>

      {featuredListings.length > 0 ? (
        <!-- Horizontal scroller: cards scroll sideways instead of stacking, keeping the homepage short.
             Negative margins let cards bleed to the screen edges while the first card stays aligned. -->
        <div class="-mx-4 sm:-mx-6 lg:-mx-8">
          <div class="flex gap-6 overflow-x-auto snap-x snap-mandatory scroll-smooth px-4 sm:px-6 lg:px-8 pb-4 home-scroller">
            {featuredListings.map(listing => (
              <div class="snap-start shrink-0 w-[280px] sm:w-[330px]">
                <ListingCard
                  slug={listing.slug}
                  title={listing.data.title}
                  status={listing.data.status}
                  acreage={listing.data.acreage}
                  county={listing.data.county}
                  state={listing.data.state}
                  price={listing.data.price}
                  photos={listing.data.photos}
                  description={listing.body}
                />
              </div>
            ))}
          </div>
        </div>
      ) : (
        <div class="text-center py-16 bg-white/40 rounded-2xl border border-bg-dark/20">
          <p class="text-bg-dark mb-4">New listings coming soon — check back often!</p>
          <a href="/find-land" class="text-bg-dark font-semibold hover:underline">Tell us what you're looking for →</a>
        </div>
      )}

      <div class="text-center mt-10 sm:hidden">
        <a href="/listings" class="text-bg-dark font-bold hover:underline">See All Listings →</a>
      </div>
    </div>
  </section>

  <!-- Newsletter / New Listings Alert -->
  <section class="py-16 bg-white border-b border-gray-100">
    <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <div class="text-3xl mb-3">📰</div>
      <h2 class="font-display font-bold text-2xl sm:text-3xl text-bg-dark mb-2">Get New Land Deals First</h2>
      <p class="text-gray-500 mb-8">Be first to see new parcels, price drops, and market updates — straight to your inbox. No spam, ever.</p>
      <div>
        <iframe
          src="https://brand.alot.land/widget/form/5OXc42k9A3Urpgu6VuNA"
          style="width:100%;height:750px;border:none;border-radius:8px"
          id="inline-5OXc42k9A3Urpgu6VuNA"
          data-layout="{'id':'INLINE'}"
          data-trigger-type="alwaysShow"
          data-trigger-value=""
          data-activation-type="alwaysActivated"
          data-activation-value=""
          data-deactivation-type="neverDeactivate"
          data-deactivation-value=""
          data-form-name="Newsletter Signup"
          data-height="568"
          data-layout-iframe-id="inline-5OXc42k9A3Urpgu6VuNA"
          data-form-id="5OXc42k9A3Urpgu6VuNA"
          title="Newsletter Signup"
        ></iframe>
        <script src="https://brand.alot.land/js/form_embed.js"></script>
      </div>
    </div>
  </section>

  <!-- How It Works: Buyer -->
  <section class="py-20 bg-bg-dark text-white">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="font-display font-bold text-3xl sm:text-4xl text-center text-white mb-12">
        How It Works — Buying Land
      </h2>
      <div class="grid sm:grid-cols-3 gap-8">
        {[
          { step: '01', icon: '📬', title: 'We Find It Off-Market', desc: 'We reach out directly to landowners through direct mail — not on MLS, not on auction sites.' },
          { step: '02', icon: '📝', title: 'We Get It Under Contract', desc: 'We negotiate directly with the owner and get the property under contract at a fair price.' },
          { step: '03', icon: '🤝', title: 'You Buy It', desc: 'We list the property with top local land realtors. You buy it at a fair, transparent price.' },
        ].map(item => (
          <div class="text-center">
            <div class="w-16 h-16 bg-gold rounded-2xl flex items-center justify-center text-2xl mx-auto mb-4">
              {item.icon}
            </div>
            <div class="text-gold font-bold text-sm mb-2">{item.step}</div>
            <h3 class="font-display font-bold text-lg text-white mb-2">{item.title}</h3>
            <p class="text-gray-400 text-sm leading-relaxed">{item.desc}</p>
          </div>
        ))}
      </div>
      <div class="text-center mt-10">
        <a href="/listings" class="bg-gold text-bg-dark font-bold px-8 py-4 rounded-2xl hover:bg-yellow-400 transition-colors inline-block">
          Browse Available Land
        </a>
      </div>
    </div>
  </section>

  <!-- How It Works: Seller -->
  <section class="py-20 bg-green-brand text-white">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="font-display font-bold text-3xl sm:text-4xl text-center mb-12">
        How It Works — Selling Land
      </h2>
      <div class="grid sm:grid-cols-3 gap-8">
        {[
          { step: '01', icon: '📋', title: 'Submit Your Property', desc: 'Fill out our quick form with your land details — location, acreage, and asking price. Takes less than 5 minutes.' },
          { step: '02', icon: '💵', title: 'Get a Cash Offer', desc: 'David personally reviews your property and sends a fair cash offer within 24 hours. No agents, no fees.' },
          { step: '03', icon: '🎉', title: 'Close & Get Paid', desc: 'We handle all the paperwork and close fast — usually in 2 weeks or less. Cash in hand, hassle-free.' },
        ].map(item => (
          <div class="text-center">
            <div class="w-16 h-16 bg-white/20 rounded-2xl flex items-center justify-center text-2xl mx-auto mb-4">
              {item.icon}
            </div>
            <div class="text-white/60 font-bold text-sm mb-2">{item.step}</div>
            <h3 class="font-display font-bold text-lg text-white mb-2">{item.title}</h3>
            <p class="text-green-100 text-sm leading-relaxed">{item.desc}</p>
          </div>
        ))}
      </div>
      <div class="text-center mt-10">
        <a href="/sell-land" class="bg-gold text-bg-dark font-bold px-8 py-4 rounded-2xl hover:bg-yellow-400 transition-colors inline-block">
          Get a Cash Offer →
        </a>
      </div>
    </div>
  </section>

  <!-- Video Section -->
  <section class="py-20 bg-bg-light">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="font-display font-bold text-3xl sm:text-4xl text-bg-dark mb-4">Hear From David</h2>
      <p class="text-gray-500 mb-8">Watch a quick overview of how we work.</p>
      <div class="aspect-video w-full rounded-2xl overflow-hidden shadow-lg">
        <iframe
          src={`https://www.youtube.com/embed/${homepageVideoId}?rel=0&modestbranding=1`}
          title="Alot of Land — How We Work"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen
          class="w-full h-full"
          loading="lazy"
        ></iframe>
      </div>
    </div>
  </section>

  <!-- Testimonials -->
  <TestimonialSection variant="dark" max={3} />

  <!-- FAQ Section -->
  <FAQSection />

  <!-- Bottom CTA -->
  <section class="py-20 bg-bg-dark text-white text-center">
    <div class="max-w-2xl mx-auto px-4">
      <h2 class="font-display font-bold text-3xl sm:text-4xl mb-4">Ready to find your land?</h2>
      <p class="text-gray-400 mb-8 text-lg">Browse current listings or reach out and tell us what you're looking for.</p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/listings" class="bg-gold text-bg-dark font-bold px-8 py-4 rounded-2xl hover:bg-yellow-400 transition-colors">
          Browse Listings
        </a>
        <a href={`tel:${phoneRaw}`} class="border-2 border-white text-white font-bold px-8 py-4 rounded-2xl hover:bg-white/10 transition-colors">
          📞 Call Us
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Thin, subtle scrollbar so the Featured Listings row reads as scrollable */
  .home-scroller { scrollbar-width: thin; scrollbar-color: rgba(10,10,10,.35) transparent; }
  .home-scroller::-webkit-scrollbar { height: 8px; }
  .home-scroller::-webkit-scrollbar-track { background: transparent; }
  .home-scroller::-webkit-scrollbar-thumb { background: rgba(10,10,10,.30); border-radius: 999px; }
  .home-scroller::-webkit-scrollbar-thumb:hover { background: rgba(10,10,10,.5); }
</style>

